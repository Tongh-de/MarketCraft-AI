const state={view:"home",skills:[],services:[],frameSource:""};
const $=(selector)=>document.querySelector(selector);
const elements={title:$("#viewTitle"),eyebrow:$("#viewEyebrow"),description:$("#viewDescription"),home:$("#homeView"),frameView:$("#frameView"),frame:$("#moduleFrame"),skillsView:$("#skillsView"),servicesView:$("#servicesView"),nav:[...document.querySelectorAll(".unified-nav button[data-view]")],refresh:$("#refreshShell"),toast:$("#shellToast"),commandForm:$("#commandForm"),command:$("#commandInput"),agentPlan:$("#agentPlan"),skillCatalog:$("#skillCatalog"),skillSearch:$("#skillSearch"),skillStatus:$("#skillCatalogStatus"),skillForm:$("#skillForm"),skillPlan:$("#skillPlanOutput"),serviceCatalog:$("#serviceCatalog"),serviceForm:$("#serviceForm"),skillNavCount:$("#skillNavCount"),serviceNavCount:$("#serviceNavCount"),homeSkillCount:$("#homeSkillCount"),homeServiceCount:$("#homeServiceCount")};
const views={home:{title:"AI 电商工作台",eyebrow:"CONVERSATIONAL COMMERCE WORKSPACE",description:"用一句话发起任务，也可以进入任意专业模块继续编辑。"},studio:{title:"商品创作",eyebrow:"PRODUCT CREATION",description:"上传商品图，生成多角度素材、模特试穿图和营销资产。",src:"/studio?embed=1"},competitors:{title:"竞品视觉分析",eyebrow:"COMPETITOR INTELLIGENCE",description:"拆解竞品视觉规律，形成原创差异化创作方案。",src:"/competitors?embed=1"},poster:{title:"AI 海报设计",eyebrow:"EDITABLE POSTER STUDIO",description:"在可编辑画布中调整商品、文案、颜色、尺寸和图层。",src:"/poster-editor?embed=1"},listing:{title:"多平台审核与上架",eyebrow:"LISTING ORCHESTRATION",description:"生成平台草稿，完成人工审核后执行幂等发布。",src:"/listing-workbench?embed=1"},performance:{title:"经营数据与 AI 优化",eyebrow:"PERFORMANCE FEEDBACK LOOP",description:"回流曝光、点击、转化、广告、退货和库存指标。",src:"/performance-insights?embed=1"},operations:{title:"订单、库存与履约",eyebrow:"COMMERCE OPERATIONS",description:"处理平台订单、库存证据、人工审批和履约执行。",src:"/dashboard?embed=1"},skills:{title:"Skill 插件中心",eyebrow:"EDITABLE SKILL REGISTRY",description:"创建、自动修订、启停并按需调用可插拔业务能力。"},services:{title:"外部服务中心",eyebrow:"EXTERNAL SERVICE ADAPTERS",description:"集中管理创作模型、电商平台、ERP 和审批接口。"}};
function escapeHtml(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}
function splitCsv(value){return value.split(/[,，]/).map((item)=>item.trim()).filter(Boolean);}
let toastTimer;function showToast(message,type="success"){clearTimeout(toastTimer);elements.toast.textContent=message;elements.toast.className=`shell-toast show${type==="error"?" error":""}`;toastTimer=setTimeout(()=>elements.toast.className="shell-toast",3200);}
async function jsonRequest(path,options={}){const response=await fetch(path,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});const payload=await response.json().catch(()=>null);if(!response.ok){const detail=Array.isArray(payload?.detail)?payload.detail.map((item)=>item.msg).join("；"):payload?.detail;throw new Error(detail||`请求失败：HTTP ${response.status}`);}return payload;}
function switchView(view,updateHash=true){if(!views[view])view="home";state.view=view;const meta=views[view];elements.title.textContent=meta.title;elements.eyebrow.textContent=meta.eyebrow;elements.description.textContent=meta.description;elements.nav.forEach((item)=>item.classList.toggle("active",item.dataset.view===view));elements.home.classList.toggle("active",view==="home");elements.frameView.classList.toggle("active",Boolean(meta.src));elements.skillsView.classList.toggle("active",view==="skills");elements.servicesView.classList.toggle("active",view==="services");if(meta.src&&state.frameSource!==meta.src){state.frameSource=meta.src;elements.frame.src=meta.src;}if(view==="skills")loadSkills();if(view==="services")loadServices();if(updateHash&&location.hash!==`#${view}`)history.pushState(null,"",`#${view}`);}
elements.nav.forEach((button)=>button.addEventListener("click",()=>switchView(button.dataset.view)));
document.querySelectorAll("[data-open]").forEach((button)=>button.addEventListener("click",()=>switchView(button.dataset.open)));
window.addEventListener("hashchange",()=>switchView(location.hash.slice(1)||"home",false));
elements.refresh.addEventListener("click",()=>{if(views[state.view].src){elements.frame.contentWindow.location.reload();}else if(state.view==="skills")loadSkills();else if(state.view==="services")loadServices();else loadOverview();showToast("当前视图已刷新");});
document.querySelectorAll("[data-prompt]").forEach((button)=>button.addEventListener("click",()=>{elements.command.value=button.dataset.prompt;elements.command.focus();}));
const moduleSteps={studio:"读取商品信息并生成创作任务",competitors:"分析竞品视觉并生成差异化 Brief",poster:"创建可编辑海报草稿",listing:"生成平台草稿并停在人工审核",performance:"读取经营指标并生成只读优化报告",operations:"读取订单与库存并生成运营建议"};
function inferModules(prompt){const candidates=[];if(/商品图|多角度|模特|素材|生成图/.test(prompt))candidates.push("studio");if(/竞品|对标/.test(prompt))candidates.push("competitors");if(/海报|poster/i.test(prompt))candidates.push("poster");if(/上架|发布|listing/i.test(prompt))candidates.push("listing");if(/数据|曝光|点击|转化|销量|roas|优化/i.test(prompt))candidates.push("performance");if(/订单|库存|履约|补货/.test(prompt))candidates.push("operations");return [...new Set(candidates.length?candidates:["studio"])];}
elements.commandForm.addEventListener("submit",(event)=>{event.preventDefault();const prompt=elements.command.value.trim();const modules=inferModules(prompt);const steps=["理解用户目标并提取商品与平台参数",...modules.map((item)=>moduleSteps[item]),"汇总结果、风险与下一步确认项"].slice(0,6);elements.agentPlan.hidden=false;elements.agentPlan.innerHTML=`<div class="plan-header"><div><div class="shell-eyebrow">AGENT EXECUTION PLAN</div><h3>已生成可执行计划</h3></div><span>等待用户启动</span></div><p class="plan-request">“${escapeHtml(prompt)}”</p><div class="plan-steps">${steps.map((step,index)=>`<div class="plan-step"><b>STEP ${String(index+1).padStart(2,"0")}</b><span>${escapeHtml(step)}</span></div>`).join("")}</div><div class="plan-footer"><p>当前只规划模块调用；发布、库存和履约等写操作仍会停在人工审核节点。</p><button type="button" data-start="${modules[0]}">打开第一个模块</button></div>`;elements.agentPlan.querySelector("[data-start]").addEventListener("click",(e)=>switchView(e.currentTarget.dataset.start));elements.agentPlan.scrollIntoView({behavior:"smooth",block:"nearest"});});
async function loadOverview(){try{const [skills,services]=await Promise.all([jsonRequest("/api/v1/platform/skill-plugins"),jsonRequest("/api/v1/platform/external-services")]);state.skills=skills;state.services=services;updateCounts();}catch(error){showToast(error.message,"error");}}
function updateCounts(){elements.skillNavCount.textContent=state.skills.length;elements.serviceNavCount.textContent=state.services.length;elements.homeSkillCount.textContent=state.skills.length;elements.homeServiceCount.textContent=state.services.length;}
async function loadSkills(){try{state.skills=await jsonRequest("/api/v1/platform/skill-plugins");updateCounts();renderSkills();}catch(error){showToast(error.message,"error");}}
function renderSkills(){const query=elements.skillSearch.value.trim().toLowerCase();const skills=state.skills.filter((item)=>JSON.stringify(item).toLowerCase().includes(query));elements.skillStatus.textContent=`${skills.length} 个可用清单 · ${state.skills.filter((item)=>item.status==="enabled").length} 个已启用`;elements.skillCatalog.innerHTML=skills.map((item)=>`<article class="skill-card"><div class="skill-head"><div class="skill-symbol">◇</div><div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.plugin_id)} · v${escapeHtml(item.version)} · ${item.built_in?"BUILT-IN":"CUSTOM"}</small></div><span class="state-tag ${item.status}">${item.status.toUpperCase()}</span></div><p>${escapeHtml(item.description)}</p><div class="tag-row">${item.capabilities.slice(0,4).map((tag)=>`<span>${escapeHtml(tag)}</span>`).join("")}</div><div class="skill-actions">${item.editable?`<button data-action="auto-edit" data-id="${item.plugin_id}">自动编辑</button><button data-action="toggle" data-id="${item.plugin_id}" data-status="${item.status==="enabled"?"disabled":"enabled"}">${item.status==="enabled"?"停用":"启用"}</button>`:'<span class="locked-note">内置 Skill 由代码版本维护</span>'}${item.status==="enabled"?`<button class="primary-action" data-action="invoke" data-id="${item.plugin_id}">测试调用</button>`:""}</div></article>`).join("")||'<p class="locked-note">没有匹配的 Skill。</p>';}
elements.skillSearch.addEventListener("input",renderSkills);
elements.skillCatalog.addEventListener("click",async(event)=>{const button=event.target.closest("button[data-action]");if(!button)return;const id=button.dataset.id;try{if(button.dataset.action==="auto-edit"){const change=window.prompt("输入希望自动修改的要求。修改后 Skill 会回到草稿态：","增加对低库存风险的判断，并要求所有建议携带数字证据");if(!change)return;await jsonRequest(`/api/v1/platform/skill-plugins/${id}/auto-edit`,{method:"POST",body:JSON.stringify({change_request:change,actor:"skill-ai-editor"})});showToast("Skill 已生成修订版本，请检查后重新启用");await loadSkills();}else if(button.dataset.action==="toggle"){await jsonRequest(`/api/v1/platform/skill-plugins/${id}/status`,{method:"POST",body:JSON.stringify({status:button.dataset.status,actor:"skill-admin"})});showToast(`Skill 已${button.dataset.status==="enabled"?"启用":"停用"}`);await loadSkills();}else{const prompt=window.prompt("输入测试调用指令：","分析这个商品的数据并给出优化建议");if(!prompt)return;const plan=await jsonRequest(`/api/v1/platform/skill-plugins/${id}/invoke`,{method:"POST",body:JSON.stringify({prompt,actor:"workspace-user"})});elements.skillPlan.hidden=false;elements.skillPlan.innerHTML=`<div class="shell-eyebrow">INVOCATION PLAN · MOCK</div><h3>${escapeHtml(plan.plugin_id)} 已生成调用计划</h3><p>能力：${plan.capabilities.map(escapeHtml).join("、")} · ${plan.approval_required?"执行前需要人工审核":"只读计划"}</p><code>${escapeHtml(plan.planned_tools.join(" → ")||"未绑定工具，仅输出规划")}</code>`;elements.skillPlan.scrollIntoView({behavior:"smooth",block:"nearest"});}}catch(error){showToast(error.message,"error");}});
elements.skillForm.addEventListener("submit",async(event)=>{event.preventDefault();const form=elements.skillForm.elements;try{await jsonRequest("/api/v1/platform/skill-plugins",{method:"POST",body:JSON.stringify({plugin_id:form.pluginId.value.trim(),name:form.name.value.trim(),description:form.description.value.trim(),instructions:form.instructions.value.trim(),triggers:splitCsv(form.triggers.value),capabilities:splitCsv(form.capabilities.value),tool_bindings:splitCsv(form.tools.value),risk_level:form.risk.value,actor:"skill-designer"})});showToast("自定义 Skill 草稿已创建");await loadSkills();}catch(error){showToast(error.message,"error");}});
async function loadServices(){try{state.services=await jsonRequest("/api/v1/platform/external-services");updateCounts();renderServices();}catch(error){showToast(error.message,"error");}}
function renderServices(){elements.serviceCatalog.innerHTML=state.services.map((item)=>`<article class="service-card"><div class="service-head"><div class="service-symbol">⌘</div><div><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.service_id)} · ${item.kind}</small></div><span class="state-tag ${item.status}">${item.mode.toUpperCase()} · ${item.status.replaceAll("_"," ").toUpperCase()}</span></div><p>${escapeHtml(item.description)}</p><div class="tag-row">${item.capabilities.slice(0,4).map((tag)=>`<span>${escapeHtml(tag)}</span>`).join("")}</div><div class="service-meta"><div><small>BASE URL</small><strong>${escapeHtml(item.base_url||"内置 Mock")}</strong></div><div><small>SECRET</small><strong>${escapeHtml(item.secret_reference||"不需要")}</strong></div></div><div class="service-actions"><button data-health="${item.service_id}">检查连接</button>${item.built_in?'<span class="locked-note">内置适配器</span>':""}</div></article>`).join("");}
elements.serviceCatalog.addEventListener("click",async(event)=>{const button=event.target.closest("button[data-health]");if(!button)return;try{const result=await jsonRequest(`/api/v1/platform/external-services/${button.dataset.health}/health`,{method:"POST"});showToast(result.message,result.checked?"success":"error");}catch(error){showToast(error.message,"error");}});
elements.serviceForm.addEventListener("submit",async(event)=>{event.preventDefault();const form=elements.serviceForm.elements;try{await jsonRequest("/api/v1/platform/external-services",{method:"POST",body:JSON.stringify({service_id:form.serviceId.value.trim(),name:form.name.value.trim(),kind:form.kind.value,description:form.description.value.trim(),base_url:form.baseUrl.value.trim()||null,auth_type:form.auth.value,secret_reference:form.secretRef.value.trim()||null,capabilities:splitCsv(form.capabilities.value),mode:form.mode.value,actor:"integration-admin"})});showToast("外部服务适配器已注册");await loadServices();}catch(error){showToast(error.message,"error");}});
switchView(location.hash.slice(1)||"home",false);loadOverview();
